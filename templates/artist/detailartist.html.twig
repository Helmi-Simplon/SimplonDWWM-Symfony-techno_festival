{% extends 'base.html.twig' %}

{% block title %}Artiste {{ artists.name }}{% endblock %}

{% block body %}
    <div class="card border-success p-3 text-white">
        <div class="card-title">
            <h1>{{ artists.name }}</h1>
        </div>
        {{ artists.description }}
        </div>
{% endblock %}
